export type StatisticsProps = {
    cases: number
    recovered: number
    deaths: number
    region: string   
}
export type NumberBoxProps = {
    title: string
    number: number
    color: string   
}
